[tool.isort]
profile = "black"

[tool.pytest.ini_options]
addopts = "--cov=wordgoal --cov-branch --cov-report=html --cov-report=term-missing:skip-covered --cov-report=xml --no-cov-on-fail"
log_cli = 1
log_cli_level = "DEBUG"
testpaths = "tests"
